[project]
name = "pricing-negotiation-bot"
version = "0.1.0"
requires-python = ">=3.12"

[tool.ruff]
select = ["E", "F", "I"]
line-length = 88
target-version = "py312"

[tool.pytest.ini_options]
testpaths = ["tests"]
verbose = true